// Online C++ compiler to run C++ program online
#include <iostream>
#include <math.h>
using namespace std;

int main() 
{
   float x;
   cout << "Введите пароль = ";
   cin >> x;
   if (x == 9583 || x == 1747)
       cout << "Доступны модули баз A,B,C";
   else
   
       if (x == 3331 || x == 7922)
           cout <<"Доступны модули баз B,C";
       else
       
           if (x == 9455 || x == 8997)
               cout <<"Доступны модули баз C";
           else
               cout <<"Неправильный пароль";
}